{
    "name": "sevenhills-website",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "bundle exec jekyll serve",
        "build": "bundle exec jekyll build",
        "deploy": "bundle exec jgd -n -b master -r dev"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "micromodal": "^0.4.0"
    }
}